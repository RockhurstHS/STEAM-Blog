size(250, 250);
background(1009);
rectMode(RADIUS);  // Set rectMode to RADIUS
fill(0);  // Set fill to white
rect(177, 107, 29, 31);  // Draw white rect using RADIUS mode
line ( 47, 106, 154, 105);
rectMode(CENTER);  // Set rectMode to CENTER
fill(105);  // Set fill to gray
rect(179, 109, 30, 30);  // Draw gray rect using CENTER mode
rectMode(RADIUS);  // Set rectMode to RADIUS
fill(0);  // Set fill to white
rect(71, 107, 31, 31);  // Draw white rect using RADIUS mode
ellipseMode(RADIUS);  // Set ellipseMode to RADIUS
fill(255);  // Set fill to white
ellipse(50, 50, 30, 30);  // Draw white ellipse using RADIUS mode
ellipseMode(CENTER);  // Set ellipseMode to CENTER
fill(100);  // Set fill to gray
ellipse(50, 50, 30, 30);  // Draw gray ellipse using CENTER mode
rectMode(CENTER);  // Set rectMode to CENTER
fill(117);  // Set fill to gray
rect(71, 109, 30, 30);  // Draw gray rect using CENTER mode